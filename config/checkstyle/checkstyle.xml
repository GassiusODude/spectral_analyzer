<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Fail build on violations -->
    <property name="severity" value="error"/>

    <!-- File-level checks -->
    <module name="NewlineAtEndOfFile"/>
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- Tree-based checks -->
    <module name="TreeWalker">

        <!-- Curly brace placement -->
        <module name="RightCurly">
            <property name="option" value="same"/>
            <property name="tokens"
                      value="CLASS_DEF, METHOD_DEF, CTOR_DEF,
                             LITERAL_IF, LITERAL_FOR, LITERAL_WHILE,
                             LITERAL_DO, LITERAL_TRY, LITERAL_CATCH,
                             LITERAL_FINALLY, LITERAL_ELSE"/>
        </module>

        <module name="LeftCurly">
            <property name="option" value="eol"/>
        </module>

        <!-- Whitespace -->
        <module name="WhitespaceAfter">
            <property name="tokens" value="COMMA, SEMI"/>
        </module>

        <module name="WhitespaceAround">
            <property name="tokens"
                      value="ASSIGN, BAND, BOR, BXOR,
                             DIV, EQUAL, GE, GT, LAND,
                             LE, LT, MINUS, MOD, NOT_EQUAL,
                             PLUS, QUESTION, SL, SR, STAR"/>
        </module>

        <!-- Naming -->
        <module name="MethodName"/>
        <module name="MemberName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>

        <!-- Constants -->
        <module name="ConstantName">
            <property name="format" value="^[A-Z][A-Z0-9_]*$"/>
        </module>

        <module name="ParameterName"/>
        <module name="LocalVariableName"/>

        <!-- Imports -->
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>

        <!-- Misc -->
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="NeedBraces"/>

    </module>
</module>
